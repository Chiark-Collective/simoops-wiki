---
---

# Gotchas

| Gotcha | Domain | Status | Found | Resolved / Mitigated By | Description |
|---|---|---|---|---|---|
| PostGIS password immutability | Infra | Open | 2026-05-02 | — | Once `pgdata` is initialized, changing `POSTGRES_PASSWORD` has no effect. The data directory retains the original password. Rotate via `ALTER USER` or rebuild volume. |
| Keycloak redirect URI loss | Infra | Open | 2026-05-02 | — | Keycloak container restart without persistent `kc_data` loses configured redirect URIs. Externalize realm config or mount persistent volume. |
| Keycloak OOM | Infra | Open | 2026-05-02 | — | Keycloak fails at 1 GB limit. Allocate >=2 GB in production. `db7e54e0` caps JVM heap at 768m and bumps container mem_limit to 1500m. |
| Contractor tokens deprecated | Backend | Open | 2026-05-02 | — | Contractor tokens (`sub` prefix `"contractor:"`) are rejected with 401 in all auth paths. Use Keycloak OIDC login only. |
| Planning cycle state transitions | Backend | Open | 2026-05-02 | — | Some transitions are irreversible (e.g. active -> archived). Validate before applying; no rollback path in current implementation. |
| Clash engine recomputation scope | Backend | Open | 2026-05-02 | — | Clash detection recomputes per site + shift + time slice. Large sites with many tokens may trigger expensive recalculations on every entity mutation. |
| RBAC silent failures in filtering | Backend | Resolved | 2026-05-02 | `4d32208c` | `get_site_contractor_filter` and `get_entity_visibility_filter` now return a discriminated union `EntityFilter = AdminFilter \| NoAccess \| ContractorFilter`. `NoAccess` produces `WHERE FALSE`, so ungated callers can no longer leak rows. |
| WebSocket stale permission caches | Backend | Open | 2026-05-02 | — | Cached `site_role`, `site_contractor_id`, and `can_view_others` on `WebSocketConnection` are set at subscribe time and NEVER auto-refreshed. Any DB mutation that changes these MUST call `invalidate_subscription_context` or `invalidate_user_context`. |
| Membership delete ordering | Backend | Open | 2026-05-02 | — | When removing a `SiteMembership`, call `invalidate_user_context` **before** deleting the row. If you delete first, `_filter_event_for_user` will deny the `context_invalidated` event because the membership is gone. **Latent bug:** `membership_service.py::MembershipService.reject` at line 80 deletes the membership with no preceding invalidate. Verified as a real permission-correctness issue. |
| WebSocket ephemeral events are local-only | Backend | Open | 2026-05-02 | — | `broadcast_ephemeral` does NOT publish to Redis pub/sub. In multi-process deployments, ephemeral drag/resize events only reach subscribers on the same worker. |
| Unknown audience types fail closed | Backend | Open | 2026-05-02 | — | `_audience_admits` returns `False` for unrecognised `AudienceType` values. Adding a new audience type without updating the handler silently drops all events using it. Partially mitigated by `54ad568f`: exhaustiveness contract test enumerates `get_args(AudienceType)` and asserts each value has a handler. |
| Redis pub/sub no auto-reconnect | Backend | Resolved | 2026-05-02 | `65f45a6a` | `_listen` is now a supervisor with reconnect-backoff (1s->60s, exponential, capped). Backoff resets if the inner loop processed at least one message before crashing. `is_healthy` distinguishes "running and connected" from "listener still alive". |
| Redis publish silently drops | Backend | Partially Resolved | 2026-05-02 | `65f45a6a` | `publish` now increments `_dropped_count` and warns on a logarithmic schedule (1, 2, 4, 8, ...). Messages are still lost when `_redis is None`; there is no requeue. |
| Redis event log clear() unguarded | Backend | Resolved | 2026-05-02 | `b8ea3ec5` + `65f45a6a` | `clear()` raises when `environment != "test"`. Key prefixes are now env-scoped (`simoops:{env}:events:`), so a misfired test command cannot nuke production. |
| Data lock timezone stripping | Backend | Resolved | 2026-05-02 | `ad72fbf4` | `_to_aware_utc` converts aware inputs to UTC via `astimezone` and tags naive inputs as UTC. `replace(tzinfo=None)` is no longer used. |
| Entity broadcast used by planning for clash invalidation | Backend | Resolved | 2026-05-02 | `30128e01` | `invalidate_clash_cache` moved to `services/clash/clash_cache.py`. Direct callers import from there; the tight coupling between entity broadcast and planning is eliminated. |
| Schedule reconcile drops orphaned occurrences | Backend | Open | 2026-05-02 | — | `schedule_reconcile.py` removes occurrences that no longer match any shift window. Clients may see entities disappear from shifts without an explicit delete event. |
| Vertex op buffer overflow | Backend | Open | 2026-05-02 | — | `vertex_op_buffer.py` has a fixed-size ring buffer for pending ops. High-frequency edits from multiple users may evict ops before they are persisted. |
| Clash cache generation mismatch | Backend | Open | 2026-05-02 | — | `clash_cache.py` keys results by `(site_id, shift_id, generation)`. If generation is incremented but cache is not cleared, stale results are served. Always invalidate cache when incrementing generation. |
| Planning baseline re-import is not always idempotent | Backend | Open | 2026-05-02 | — | `import_baseline_service.py` shadows baseline rows. Re-running on a cycle with native modifications may create duplicates or lose edits. Re-import is safe only on fresh cycles. |
| Report provider order is hard-coded | Backend | Resolved | 2026-05-02 | `8738a273` | `ContextProvider` Protocol now declares `requires` and `provides` ClassVar tuples. `registry.py` builds run order via `graphlib.TopologicalSorter` and raises on duplicate key, missing producer, or cycle. |
| Weather cache short TTL | Backend | Open | 2026-05-02 | — | `weather/cache.py` caches Open-Meteo responses with a short TTL (15m current / 1h forecast / 5m timeline). Repeated report exports within the same hour re-fetch weather. Left as-is (`f36da393`); rationale comments at the constants explain Open-Meteo is the free/unkeyed tier and rate-limits per-IP, so global TTL is the right level. |
| Presence heartbeat required | Backend | Open | 2026-05-02 | — | `presence_manager.py` drops users whose heartbeat expires. Clients that stop sending `presence_heartbeat` will disappear from the presence list even if the WebSocket is still connected. |
| Pre-migration audit entries cannot be reverted | Backend | Open | 2026-05-02 | — | `AuditRevertService` raises HTTP 400 when the target `AuditLog.snapshot` is `None`. Any audit row written before the snapshot column was backfilled is non-revertible. |
| Audit-revert geometry SRID fallback | Backend | Resolved | 2026-05-02 | `9d6ac591` | `_deserialize_field_value` now raises on missing SRID instead of silently defaulting to `3857`. |
| Text label delete bypasses broadcast helper | Backend | Resolved | 2026-05-02 | `c89b84c2` | `delete_text_label` now routes through `_broadcast_event` instead of calling `ws_manager.broadcast_entity_event` directly. |
| JWKS stale cache on prolonged outage | Backend | Resolved | 2026-05-02 | `07feff64` | `get_jwks_cache_age_seconds()` and `is_jwks_serving_stale()` surface cache age. `/health/ready` reports `jwks` component and flips to 503 when age > 2xTTL (10 min) or serving stale. Warns on fresh->stale transition, INFO on recovery. |
| JWKS relaxed issuer boundary | Backend | Resolved | 2026-05-02 | `963a5d54` | `SIMOOPS_KEYCLOAK_VERIFY_ISSUER` env knob (default `False`) allows operators to enable issuer verification without a code change once the canonical Keycloak origin is stable. |
| Storage health transient false negatives | Backend | Resolved | 2026-05-02 | `07feff64` | `check_health` now returns `Literal["ok", "unreachable", "misconfigured"]`. Permanent `ClientError` codes (`NoSuchBucket`, `AccessDenied`, etc.) are classified as misconfigured; the rest stay unreachable. |
| Storage silent delete failures | Backend | Resolved | 2026-05-02 | `47cd368e` + `0fc7f189` | `delete_file_no_error` is now async and inserts a `PendingStorageDelete` row on failure. `storage_sweep.py` retries every 60s with exponential backoff capped at 24h. |
| Storage delete retry queue backlog | Backend | Open | 2026-05-03 | — | `pending_storage_deletes` backlog >= 1000 flips `/health/ready` to 503. Operators should monitor the `pending_storage_deletes` component to catch runaway accumulation before storage costs spike. |
| TIMESTAMPTZ migration for feature_versions | Backend | Open | 2026-05-03 | — | Migration `096` alters `feature_versions.start_at` / `end_at` from `TIMESTAMP` to `TIMESTAMPTZ`. Pre-migration, writes silently stripped timezone offsets; reads serialised naive datetimes that the frontend parsed as runner-local, causing `isEntityActiveAt` to reject features in revision mode on non-UTC runners. |
| Zone.js + MapLibre symbol-layer corruption | Frontend | Mitigated | 2026-05-02 | `bfcbcb21` / `62e111d3` | GeoJSON sources that start empty and are later populated via `setData` silently fail for symbol layers. `RecreatableMapSource` owns the recreate-on-first-populate dance and replays filter/layout/paint/feature-state across recreations. The underlying MapLibre/Zone.js bug still exists; new symbol-layer sources should use `RecreatableMapSource`. |
| MapLibre handlers fire outside Angular zone | Frontend | Mitigated | 2026-05-02 | `bfcbcb21` | `ngZone.runOutsideAngular` during map construction is required for 60fps performance, but all pointer/drag/hover handlers fire outside the zone. `mapEventSignal<T>` bridges MapLibre events into Angular signals, bypassing `markForCheck` races. |
| AuthService test token read once at construction | Frontend | Open | 2026-05-02 | — | `window.__SIMOOPS_TEST_TOKEN__` is read in the `AuthService` constructor. Injecting it after bootstrap has no effect. Set the token before `bootstrapApplication` resolves. |
| UserService removes pending invite before API resolves | Frontend | Resolved | 2026-05-02 | `99327921` | `fetchCurrentUser` defers `localStorage.removeItem` for `simoops_pending_invite_token` until `acceptInviteLink` resolves. `catchError` leaves both keys in place so the next `/me` retries the acceptance. |
| Dashboard wiring subscriptions are app-scoped | Frontend | Resolved | 2026-05-02 | `6871ba37` | `DashboardBootstrapWiringService` and `DashboardInteractionWiringService` now use component-scoped providers on `DashboardComponent`. `DestroyRef` is the dashboard's, so subscriptions unsubscribe automatically when the dashboard is destroyed. |
| PanelStateService silently swallows parse failures | Frontend | Resolved | 2026-05-02 | `b7c3b270` | `JSON.parse` failures now warn via `createLogger('PanelState')` with truncated raw value. Per-field validation drops also emit warnings. |
| WebSocket catch-up dedup races with live events | Frontend | Open | 2026-05-02 | — | `pendingCatchUpSnapshot` is frozen at `sendCatchUp` time. If a live broadcast arrives after send but before the response with `seq > snapshot`, the snapshot threshold prevents silent dropping of legitimately missed events. See `websocket.service.ts::sendCatchUp` "C2 regression" comment. |
| Offline queue optimistic state may drift | Frontend | Partially Resolved | 2026-05-02 | `33216699` | `executeMutation` now suppresses WS delta application during in-flight mutations and rolls back on 500. If the API succeeds but the WS broadcast is delayed, local state may still be temporarily out of sync with the server seq. |
| Map source recreation resets filters and layout properties | Frontend | Resolved | 2026-05-02 | `bfcbcb21` | `RecreatableMapSource` records `setFilter` / `setFeatureState` / `setLayoutProperty` / `setPaintProperty` in a typed replay log and replays them on recreation. Callers no longer need to re-apply state manually. |
| SiteContextService getters return new Observable references | Frontend | Resolved | 2026-05-02 | `ebfa43aa` | `selectedShift$` and `selectedDate$` are now `readonly` fields, not getters. The synchronous `selectedShift`/`selectedDate` getters remain for fresh reads. |
| Sync bindings must be injected eagerly | Frontend | Mitigated | 2026-05-02 | `4bdd36cb` | `ContractorSyncBinding`, `ShiftSyncBinding`, `InviteSyncBinding`, and others must be injected before the first WS broadcast arrives. Lazy injection (e.g. behind `*ngIf`) misses early events. All nine bindings now carry explicit "Materialised once at app start" comments forbidding lazy injection. |
| EntityStore optimistic updates only for tokens and plants | Frontend | Partially Resolved | 2026-05-02 | `4539e12d` + `6c05ffe2` | `SyncCoordinator<T>` now provides optimistic snapshot, 409 rollback, and WS dedup uniformly across tokens, plants, areas, deliveries, POIs, text-labels, and alerts. The design-flag cluster G7 remains open for a full `EntitySyncEngine` abstraction. |
| Revision mode bypasses plan-state filter | Frontend | Resolved | 2026-05-02 | `dbcb7815` | `ViewModeService.shouldBypassPlanFilter$` gates the bypass for all read-only modes (revision + viewing_submitted + compare). Inconsistent combined states are now unrepresentable. |
| Clash state is a no-op in revision mode | Frontend | Resolved | 2026-05-02 | `dbcb7815` | `ViewModeService.shouldDropLiveEvents$` gates clash drops for all read-only modes. `ClashStateService.setClashes` now warns via the service logger on each drop instead of going silent. |
| ViewModeService setAppMode no-op during revision | Frontend | Open | 2026-05-02 | — | `setAppMode` is a silent no-op while revision is active. Callers that need to re-apply a mode after exiting revision must trigger the transition separately. The cycle-driven default logic does this automatically. |
| RecreatableMapSource replay log growth | Frontend | Open | 2026-05-02 | — | `RecreatableMapSource` accumulates every `setFilter` / `setFeatureState` / `setLayoutProperty` / `setPaintProperty` call in its replay log. High-frequency dynamic updates (e.g. per-frame hover effects) can cause unbounded log growth. Use `removeFeatureState` to clear entries that should not replay. |
| WebSocket presence vs database presence | General | Open | 2026-05-02 | — | User may be present in WebSocket room but not reflected in database presence table if Redis is down and fallback event buffer overflows. Do not rely solely on presence table for safety-critical checks. |
| VPS memory overcommit | Infra | Open | 2026-05-07 | — | Container mem_limits total ~9.84 GB on a 7.7 GB VPS. Zero swap. OOM killer terminates gunicorn and redis repeatedly. See [analysis](analyses/oom-kill-cascade.md) and [Compute](infra/compute.md). |
| No offsite backup | Infra | Open | 2026-05-07 | — | `backup.sh` only covers PostgreSQL; `rclone copy` for offsite is commented out. Minio COGs have no backup at all. VPS disk failure loses both data and backups. See [Backup/restore runbook](ops/backup-restore.md). |
| Deploy drops WebSocket connections | Infra | Open | 2026-05-07 | — | `make prod-up` causes ~10-30s downtime. All WebSocket connections are dropped. Zero-downtime deploy (blue-green, rolling) is not implemented. See [Build](build.md). |
| DB session rollback cancellation | Backend | Open | 2026-05-07 | — | `get_session()` `finally` block now wraps `session.rollback()` in `asyncio.shield()` because `CancelledError` during client disconnect can interrupt rollback mid-flight, leaving the connection in `idle (ClientRead)` state and exhausting the pool. |
| Clash compute pool exhaustion | Backend | Mitigated | 2026-05-07 | `ea9c6000` | N concurrent `/api/clashes` requests for the same key each spawned their own heavy compute. Per-key `asyncio.Lock` coalescing + 15s timeout cap prevents pool exhaustion on large sites. |
| Orphan feature versions | Backend | Resolved | 2026-05-07 | `408d8e57` | Migration 097 closes orphan `feature_versions` (`valid_to = valid_from`) caused by legacy data migrations seeding versions before matching feature rows existed. Fixes phantom zone entries in `at_time` queries. |
| Cross-contractor cache invalidation | Backend | Open | 2026-05-07 | — | `context_invalidated` broadcast after invite acceptance may race with concurrent WebSocket events. The stale cache persists until the next subscribe or a manual page refresh. Best-effort try/except wrap on invalidation. |
| MapLibre source recreation log growth | Frontend | Resolved | 2026-05-07 | `f3714347` | `RecreatableMapSource` replay log now capped at 10,000 entries with warning at 5,000. Prevents unbounded growth from pathological hot loops (e.g. per-frame `setFeatureState`). |
| Auth-diag unauthenticated endpoint | Backend | Open | 2026-05-07 | — | `POST /api/diag/auth-event` accepts unauthenticated requests by design (the auth flow is what's being diagnosed). Bounded harm: only writes log entries. No rate limiting yet; gate behind `core.limiter` if volume spikes. |
| Identity flip on silent renew | Frontend | Open | 2026-05-07 | — | `AuthService` detects `sub` changes during silent renew and logs `[AUTH-DIAG] !! identity flipped`. `SiteContextService` now scopes `last_site_id` to `userSub` to prevent cross-account site inheritance. |
| Keycloak theme static assets | Infra | Resolved | 2026-05-07 | `f6182806` | Caddy now routes `/resources/*` and `/js/*` to Keycloak. Previously these fell through to the Angular SPA catch-all, causing unstyled login pages ("90s white form" symptom). |
| Keycloak ephemeral data loss | Infra | Resolved | 2026-05-07 | `7a0b5432` | `keycloak-data` named volume mounted at `/opt/keycloak/data` persists H2 database, sessions, and theme settings across container restarts. Previously all state was lost on every `docker compose up`. Note: H2 only used in dev; production uses PostgreSQL via `KC_DB: postgres`. |
| Site logo bypasses normalisation | Backend | Open | 2026-05-07 | — | Site logo upload route imports `ALLOWED_LOGO_TYPES` and `MAX_LOGO_SIZE` from `logo_normalisation` for validation but uploads **raw unnormalised bytes**. No resize, no RGBA conversion, no SVG script check. Contractor logo route uses full normalisation. Divergence not yet unified. |

## Legend

| Status | Meaning |
|---|---|
| **Open** | No fix committed. Still a live footgun. |
| **Resolved** | Code fix committed and verified. Retained for archival traceability. |
| **Mitigated** | Workaround in place. Underlying framework/behaviour issue still exists. |
| **Partially Resolved** | Improvement committed, but edge cases or design debt remain. |

## Design-flag clusters

| ID | Topic | Status | Related gotchas |
|---|---|---|---|
| G7 | Optimistic update / WS sync engine abstraction | Open | EntityStore optimistic updates, Offline queue drift |
| G8 | WebSocket auth-state contract refresh | Open | WS stale permission caches |
| G9 | Redis production readiness (requeue, multi-AZ) | Open | Redis publish silently drops |
| G10 | MapLibre source abstraction (intermediate layer) | Open | Zone.js + MapLibre symbol-layer corruption, MapLibre handlers fire outside Angular zone |
